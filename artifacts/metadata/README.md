# Metadados de aquisição

`acquisition_metadata.json` foi produzido por leitura direta dos membros MP4 contidos no ZIP, sem extração de frames. Os avisos do `ffprobe` decorrem da leitura sequencial de um contêiner MP4 por entrada não-seekable e são preservados, com endereços voláteis normalizados.
